specVersion: "v1beta"
serviceId: "api"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "api-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
